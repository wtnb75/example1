# pubspec.yaml
name: example1
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  logging: ^1.2.0
  yaml: ^3.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.2
  args: ^2.5.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - workflow/
